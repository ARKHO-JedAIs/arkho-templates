# CLAUDE.md

This file is a thin bootstrap for Claude Code and holds no rules of its own.

All repository instructions - rules, stack decisions, architectural constraints, secrets inventory, conventions, and workflow - live in `AGENTS.md`, the single source of truth for every agent harness. It is imported below so it always loads; never duplicate its content here. When a rule changes, edit `AGENTS.md` only.

@AGENTS.md
