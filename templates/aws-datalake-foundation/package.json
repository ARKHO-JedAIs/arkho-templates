{
  "name": "{{ project_slug }}-cdk",
  "version": "0.1.0",
  "description": "IaC del Data Lake Fundacional AWS para {{ client_name }} (CDK v2, TypeScript)",
  "bin": {
    "{{ project_slug }}": "bin/app.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "synth:dev": "cdk synth --quiet -c env=dev",
    "diff:dev": "cdk diff -c env=dev",
    "deploy:dev": "cdk deploy --all -c env=dev",
    "deploy:prod": "cdk deploy --all -c env=prod --require-approval broadening",
    "nag": "cdk synth --quiet -c env=dev -c nag=true"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "20.14.9",
    "aws-cdk": "^2.170.0",
    "cdk-nag": "^2.34.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2"
  }
}
