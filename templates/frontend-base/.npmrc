# Make the `engines` floor in package.json an actual gate rather than a warning:
# a mismatched Node version fails `npm install` with a clear message instead of
# surfacing later as a confusing build error.
engine-strict=true
