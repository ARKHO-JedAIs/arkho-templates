# Normalize line endings on checkout so a Windows clone does not fight Prettier
# (endOfLine: "lf"). Without this, every file a Windows dev touches shows up as
# fully rewritten in the diff.
* text=auto eol=lf
*.png binary
*.jpg binary
*.woff2 binary
